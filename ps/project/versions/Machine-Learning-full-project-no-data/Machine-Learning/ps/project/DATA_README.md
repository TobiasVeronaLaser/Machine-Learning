# Dataset location

Place the training dataset here before running the notebooks:

```text
Machine-Learning/ps/project/sign_lang_train/sign_lang_train/
├── labels.csv
└── *.jpg
```

The dataset images and labels are intentionally not included in this ZIP.
